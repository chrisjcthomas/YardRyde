{
  "name": "real-time-transit-tracker-unified",
  "version": "1.0.0",
  "description": "Unified deployment for Real-Time Transit Tracker",
  "private": true,
  "scripts": {
    "build:client": "cd client && npm install && npm run build",
    "build:server": "cd server && ./mvnw clean package -DskipTests",
    "build": "npm run build:client && rm -rf server/src/main/resources/static/* && cp -r client/dist/* server/src/main/resources/static/ && npm run build:server"
  }
}
